package com.github.sobornovdev.caravans;

/**
 * Root aggregate of the simulation.
 *
 * <p>Owns the map, villages, roads, caravans, and player state.
 * All concurrency-sensitive state lives behind methods on {@code World}
 * and the entities it owns.
 *
 * <p>This is intentionally a stub. Implementation is your job —
 * the plan lives in {@code 02_architecture_decisions.md} and
 * {@code 03_roadmap.md}.
 */
public final class World {
    // Iteration 1: fill me in.
}
